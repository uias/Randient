//
//  UIGradient.swift
//  Randient
//
//  Auto Generated by UI At Six.
//  Gradients provided courtesy of uigradients.com.
//

import UIKit

extension UIGradient {

    public struct Data {
        public let name: String
        public let colors: [UIColor]
    }
}

public enum UIGradient: CaseIterable {
    <% @colors.each do |color| %>case <%=color["camelized_name"]%>
    <% end -%>
}

extension UIGradient {

    public var data: Data {
        switch self {
            <% @colors.each do |color| %>case .<%=color["camelized_name"]%>:
                return Data(name: "<%=color["name"]%>", 
                            colors: [<% color["colors"].each do |hexString| %>ColorParser.parse("<%=hexString %>"), <% end %>])
            <% end %>
        }
    }
}

private class ColorParser {
    
    class func parse(_ hex: String) -> UIColor {
        var cString:String = hex.trimmingCharacters(in: .whitespacesAndNewlines).uppercased()

        if (cString.hasPrefix("#")) {
            cString.remove(at: cString.startIndex)
        }

        if ((cString.count) != 6) {
            return UIColor.gray
        }

        var rgbValue:UInt64 = 0
        Scanner(string: cString).scanHexInt64(&rgbValue)

        return UIColor(
            red: CGFloat((rgbValue & 0xFF0000) >> 16) / 255.0,
            green: CGFloat((rgbValue & 0x00FF00) >> 8) / 255.0,
            blue: CGFloat(rgbValue & 0x0000FF) / 255.0,
            alpha: CGFloat(1.0)
        )
    }
}
